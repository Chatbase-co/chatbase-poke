import { TaskLayout } from "@/components/task-layout"

export default function IndexPage() {
  /**
   * You can remove the TaskLayout component it only contains the
   * the info about the task
   * you can always look at the `/readme` page if you need to
   * check the requirements again
   */
  return <TaskLayout></TaskLayout>
}
